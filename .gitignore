# ============================================================
# sea_salt_room_web · Git 忽略规则
# 目标：只纳入「源码 + 依赖」，模型文件(GLB)不纳入版本库。
# 理由：GLB 体积大(共 ~309MB)，且用户决定不推模型文件到 GitHub；
#       模型仅保留在本地磁盘，页面运行时从本地读取（需 http 访问）。
# 注意：克隆本仓库后页面不能直接运行，需自行补回 sea_salt_room_web 下的
#       6 个 .glb（房间/家具/房子/邮箱）。
# ============================================================

# ---- 系统 / 隐藏文件 ----
.DS_Store

# ---- 所有 GLB 模型文件（不纳入版本库，仅本地保留）----
*.glb

# ---- 历史调试残渣（9-08 一次性素材，未被任何代码引用）----
*.png
*.jpg
*.svg
__prev*.html
